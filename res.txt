import os


archiver = '"""\narchiver.py — Создание RAR архивов с авто-разбивкой на тома.\n\nТребует установленного WinRAR или rar (Linux/macOS):\n  Windows : C:\\Program Files\\WinRAR\\rar.exe  (по умолчанию)\n  Linux   : sudo apt install rar  /  brew install rar\n"""\n\nimport os\nimport subprocess\nimport glob\nimport shutil\nfrom pathlib import Path\nfrom config import SPLIT_THRESHOLD_MB, VOLUME_SIZE_MB, DONE_DIR\n\n# ❌ БЫЛО (вызов до определения функции → NameError):\n# RAR_EXECUTABLE = _find_rar()\n#\n# ✅ СТАЛО: _find_rar() вызывается внутри create_rar_archive()\n#    при каждом создании архива — никакой глобальной переменной\n\n\ndef _find_rar() -> str:\n    """\n    Найти исполняемый файл rar/WinRAR в системе.\n\n    Проверяет стандартные пути Windows, затем PATH (Linux/macOS).\n    Вызывается внутри create_rar_archive() при каждом запуске.\n\n    Returns:\n        str — путь к исполняемому файлу rar\n\n    Raises:\n        EnvironmentError — если rar не найден нигде\n    """\n    candidates = [\n        r\'C:\\Program Files\\WinRAR\\rar.exe\',\n        r\'C:\\Program Files (x86)\\WinRAR\\rar.exe\',\n        \'rar\',    # Linux/macOS — ищет в PATH\n        \'rar5\',   # некоторые дистрибутивы Linux\n    ]\n    for c in candidates:\n        if os.path.isfile(c):\n            return c\n        if shutil.which(c):\n            return c\n    raise EnvironmentError(\n        "❌ rar/WinRAR не найден!\\n"\n        "   Windows: установите WinRAR → https://www.rarlab.com\\n"\n        "   Linux  : sudo apt install rar\\n"\n        "   macOS  : brew install rar"\n    )\n\n\ndef create_rar_archive(file_path: str, output_dir: str) -> list[str]:\n    """\n    Создать RAR архив из файла.\n\n    Логика:\n      - файл < SPLIT_THRESHOLD_MB  → один архив filename.rar\n      - файл >= SPLIT_THRESHOLD_MB → тома filename.part1.rar, .part2.rar, ...\n\n    Args:\n        file_path : полный путь к исходному файлу\n        output_dir: папка для сохранения архива/томов\n\n    Returns:\n        list[str] — отсортированный список путей к созданным .rar файлам\n    """\n    # _find_rar() вызывается здесь — после определения функции ✅\n    rar_exe = _find_rar()\n\n    file_path  = Path(file_path)\n    output_dir = Path(output_dir)\n    output_dir.mkdir(parents=True, exist_ok=True)\n\n    file_size_mb = file_path.stat().st_size / (1024 * 1024)\n    archive_name = file_path.stem        # имя без расширения\n    archive_path = output_dir / f"{archive_name}.rar"\n\n    if file_size_mb < SPLIT_THRESHOLD_MB:\n        # ── Один архив без разбивки ─────────────────\n        cmd = [\n            rar_exe,\n            \'a\',               # add\n            \'-ep\',             # не хранить пути внутри архива\n            \'-m5\',             # максимальное сжатие\n            \'-y\',              # yes to all (без интерактивных вопросов)\n            str(archive_path),\n            str(file_path),\n        ]\n    else:\n        # ── Разбивка на тома по VOLUME_SIZE_MB МБ ───\n        cmd = [\n            rar_exe,\n            \'a\',\n            \'-ep\',\n            \'-m5\',\n            \'-y\',\n            f\'-v{VOLUME_SIZE_MB}m\',  # размер одного тома\n            str(archive_path),\n            str(file_path),\n        ]\n\n    result = subprocess.run(\n        cmd,\n        capture_output=True,\n        text=True,\n        encoding=\'utf-8\',\n        errors=\'replace\'\n    )\n\n    if result.returncode != 0:\n        raise RuntimeError(\n            f"❌ Ошибка RAR (код {result.returncode}):\\n{result.stderr}"\n        )\n\n    # Собрать все созданные части (*.rar) в папке\n    pattern = str(output_dir / f"{archive_name}*.rar")\n    parts   = sorted(glob.glob(pattern))\n\n    if not parts:\n        raise RuntimeError(\n            f"❌ RAR завершился без ошибок, но архив не найден: {pattern}"\n        )\n\n    # Переместить исходный файл в done_files/\n    done = Path(DONE_DIR)\n    done.mkdir(parents=True, exist_ok=True)\n    shutil.move(str(file_path), str(done))\n\n    return parts\n'

config = '"""\nconfig.py — Настройки приложения.\nИзмените значения под свои нужды.\n"""\n\n# ────────────────────────────────────────────────\n# Пути — ОТПРАВИТЕЛЬ\n# ────────────────────────────────────────────────\n\n# Файлы для обработки и отправки\nINPUT_DIR = \'./input_files\'\n\n# Готовые зашифрованные .txt (можно взять вручную или загрузить в GitHub)\nENCODED_DIR = \'./encoded_files\'\n\n# Временные RAR архивы (удаляются после кодирования если CLEANUP_TEMP=True)\nTEMP_RAR_DIR = \'./temp_rar\'\n\n# Исходные файлы после обработки переносятся сюда\nDONE_DIR = \'./done_files\'\n\n# ────────────────────────────────────────────────\n# Пути — ПОЛУЧАТЕЛЬ\n# ────────────────────────────────────────────────\n\n# Скачанные / принятые .txt файлы (вход для декодера)\nRECEIVED_TXT_DIR = \'./received_txt\'\n\n# Промежуточные .rar файлы после декодирования\nDECODED_RAR_DIR = \'./decoded_rar\'\n\n# Итоговые распакованные файлы\nOUTPUT_DIR = \'./output_files\'\n\n# ────────────────────────────────────────────────\n# Параметры архивации\n# ────────────────────────────────────────────────\n\n# Если файл >= порога — разбить на тома (МБ)\nSPLIT_THRESHOLD_MB = 25\n\n# Размер одного тома при разбивке (МБ)\nVOLUME_SIZE_MB = 15\n\n# Удалять временные .rar после кодирования?\nCLEANUP_TEMP = True\n\n# ────────────────────────────────────────────────\n# GitHub настройки\n#\n# Получить токен:\n#   GitHub → Settings → Developer settings\n#   → Personal access tokens → Fine-grained tokens\n#   → Repository permissions → Contents: Read and write\n#\n# ⚠️  Добавьте config.py в .gitignore — не публикуйте токен!\n# ────────────────────────────────────────────────\n\nGITHUB_TOKEN    = \'\'\nGITHUB_USERNAME = \'Katabym\'\nGITHUB_REPO     = \'FileDroper\'\n\n# Папка внутри репо для загрузки/скачивания файлов\nGITHUB_UPLOAD_DIR = \'uploads\'\n\n# Ветка репозитория\nGITHUB_BRANCH = \'main\'\n'

decoder = '"""\ndecoder.py — Декодирование и распаковка файлов.\n\nВызывается из main.py (режим получателя) или напрямую:\n  python decoder.py\n\nОжидает .txt файлы в RECEIVED_TXT_DIR (config.py).\nИтоговые файлы сохраняет в OUTPUT_DIR.\nДля распаковки RAR нужен WinRAR или unrar.\n"""\n\nimport base64\nimport json\nimport os\nimport glob\nimport shutil\nimport subprocess\nfrom pathlib import Path\nfrom config import RECEIVED_TXT_DIR, DECODED_RAR_DIR, OUTPUT_DIR\n\n\ndef _setup():\n    for d in [RECEIVED_TXT_DIR, DECODED_RAR_DIR, OUTPUT_DIR]:\n        Path(d).mkdir(parents=True, exist_ok=True)\n\n\ndef _find_rar_tool() -> str:\n    """Найти unrar или rar для распаковки."""\n    candidates = [\n        r\'C:\\Program Files\\WinRAR\\unrar.exe\',\n        r\'C:\\Program Files\\WinRAR\\rar.exe\',\n        \'unrar\', \'rar\',\n    ]\n    for c in candidates:\n        if os.path.isfile(c) or shutil.which(c):\n            return c\n    raise EnvironmentError("❌ unrar/WinRAR не найден! Установите и добавьте в PATH.")\n\n\ndef _decode_txt_to_rar(txt_path: str) -> str | None:\n    """Декодировать один .txt файл обратно в .rar."""\n    try:\n        with open(txt_path, \'r\', encoding=\'utf-8\') as f:\n            payload = json.load(f)\n\n        filename  = payload[\'filename\']\n        rar_bytes = base64.b64decode(payload[\'filedata\'])\n\n        rar_path = Path(DECODED_RAR_DIR) / filename\n        with open(rar_path, \'wb\') as f:\n            f.write(rar_bytes)\n\n        size_kb = len(rar_bytes) / 1024\n        print(f"   ✅ Декодирован: {filename}  ({size_kb:.1f} КБ)")\n        return str(rar_path)\n\n    except (KeyError, json.JSONDecodeError) as e:\n        print(f"   ❌ Ошибка чтения {Path(txt_path).name}: {e}")\n        return None\n\n\ndef _extract_rar(rar_path: str) -> bool:\n    """Распаковать .rar архив в OUTPUT_DIR. Возвращает True при успехе."""\n    tool = _find_rar_tool()\n    # unrar e archive.rar ./output/ -y  (-y = yes to all prompts)\n    cmd = [tool, \'e\', rar_path, str(Path(OUTPUT_DIR)) + \'/\', \'-y\']\n    result = subprocess.run(cmd, capture_output=True, text=True)\n    if result.returncode != 0:\n        print(f"   ⚠️  Ошибка распаковки: {result.stderr[:200]}")\n        return False\n    print(f"   📦 Распаковано → {OUTPUT_DIR}/")\n    return True\n\n\ndef run_decoder() -> bool:\n    """\n    Основная функция декодирования.\n\n    Читает .txt из RECEIVED_TXT_DIR, восстанавливает .rar,\n    распаковывает в OUTPUT_DIR.\n\n    Returns:\n        True  — если хотя бы один файл успешно распакован\n        False — при ошибке или отсутствии файлов\n    """\n    _setup()\n\n    txt_files = sorted(glob.glob(\n        os.path.join(RECEIVED_TXT_DIR, \'*.txt\')\n    ))\n\n    if not txt_files:\n        print(f"\\n⚠️  В папке {RECEIVED_TXT_DIR}/ нет .txt файлов.\\n")\n        return False\n\n    print(f"\\n📥 .txt файлов для декодирования: {len(txt_files)}")\n\n    # ШАГ 1: Декодирование .txt → .rar\n    print("\\n🔓 Декодирование Base64 → .rar ...")\n    rar_files = []\n    for txt in txt_files:\n        rar = _decode_txt_to_rar(txt)\n        if rar:\n            rar_files.append(rar)\n\n    if not rar_files:\n        print("\\n❌ Ни один файл не был декодирован.\\n")\n        return False\n\n    # ШАГ 2: Найти первые части (для многотомных архивов)\n    # WinRAR/unrar сам найдёт остальные части по имени part1\n    # Одиночный архив: name.rar (без part) → тоже берём\n    first_parts = []\n    seen_bases  = set()\n\n    for rar in sorted(rar_files):\n        stem = Path(rar).stem            # e.g. "video.part1" or "doc"\n        if \'part\' in stem:\n            # Многотомный: берём только part1\n            base = stem.rsplit(\'.\', 1)[0]   # "video"\n            if base not in seen_bases and stem.endswith(\'part01\'):\n                first_parts.append(rar)\n                seen_bases.add(base)\n        else:\n            # Одиночный архив\n            first_parts.append(rar)\n\n    # ШАГ 3: Распаковка\n    print(f"\\n📦 Распаковка ({len(first_parts)} архив(ов))...")\n    ok_count = 0\n    for rar in first_parts:\n        print(f"   Архив: {Path(rar).name}")\n        if _extract_rar(rar):\n            ok_count += 1\n\n    if ok_count > 0:\n        print(f"\\n✅ Успешно распаковано: {ok_count}/{len(first_parts)}")\n        print(f"📂 Файлы в папке: {OUTPUT_DIR}/\\n")\n        return True\n    else:\n        print("\\n❌ Распаковка не удалась.\\n")\n        return False\n\n\n# ────────────────────────────────────────────────\n# Запуск напрямую (без main.py)\n# ────────────────────────────────────────────────\nif __name__ == \'__main__\':\n    run_decoder()\n'

encoder = '"""\nencoder.py — Кодирование RAR файлов в Base64 .txt\n"""\n\nimport base64\nimport json\nimport os\nfrom pathlib import Path\nfrom config import CLEANUP_TEMP\n\n\ndef encode_to_txt(rar_parts: list[str], output_dir: str) -> list[str]:\n    """\n    Закодировать список RAR файлов в .txt (Base64 + JSON).\n\n    Каждый .rar файл → отдельный .txt файл.\n\n    Args:\n        rar_parts  : список путей к .rar файлам\n        output_dir : папка для сохранения .txt\n\n    Returns:\n        list[str] — пути к созданным .txt файлам\n    """\n    output_dir = Path(output_dir)\n    output_dir.mkdir(parents=True, exist_ok=True)\n    txt_files = []\n\n    for rar_path in rar_parts:\n        rar_path = Path(rar_path)\n        txt_filename = rar_path.stem + \'.txt\'\n        txt_path = output_dir / txt_filename\n\n        # Читаем бинарные данные\n        with open(rar_path, \'rb\') as f:\n            binary_data = f.read()\n\n        # Кодируем в Base64\n        b64_data = base64.b64encode(binary_data).decode(\'utf-8\')\n\n        # Формируем JSON пакет с метаданными\n        payload = {\n            \'filename\'    : rar_path.name,\n            \'original_name\': rar_path.stem,\n            \'size_bytes\'  : len(binary_data),\n            \'encoding\'    : \'base64\',\n            \'filedata\'    : b64_data,\n        }\n\n        # Записываем .txt\n        with open(txt_path, \'w\', encoding=\'utf-8\') as f:\n            f.write(json.dumps(payload, ensure_ascii=False))\n\n        txt_files.append(str(txt_path))\n\n        # Удалить временный RAR файл после кодирования\n        if CLEANUP_TEMP:\n            rar_path.unlink()\n\n    return txt_files\n\n\ndef get_txt_info(txt_path: str) -> dict:\n    """Прочитать метаданные из .txt файла (без декодирования)."""\n    with open(txt_path, \'r\', encoding=\'utf-8\') as f:\n        data = json.load(f)\n    return {k: v for k, v in data.items() if k != \'filedata\'}\n'

main = '\ufeff#!/usr/bin/env python3\n"""\nmain.py — Точка входа приложения FileConvert & Sender v2.1\nРежимы: Отправитель (архив → encode → GitHub push)\n        Получатель  (GitHub pull → decode → распаковка → очистка репо)\n"""\n\nimport os\nimport sys\nfrom pathlib import Path\nfrom config import INPUT_DIR, ENCODED_DIR, TEMP_RAR_DIR, DONE_DIR\nfrom archiver import create_rar_archive\nfrom encoder import encode_to_txt\n#from uploader import upload_to_github\n#from downloader import download_from_github\n#from cleaner import clean_github_repo\nfrom decoder import run_decoder\n\n\ndef setup_dirs():\n    """Создать рабочие директории если не существуют."""\n    for d in [INPUT_DIR, ENCODED_DIR, TEMP_RAR_DIR, DONE_DIR]:\n        Path(d).mkdir(parents=True, exist_ok=True)\n\n\n# ════════════════════════════════════════════════\n#  РЕЖИМ ОТПРАВИТЕЛЯ\n# ════════════════════════════════════════════════\n\ndef sender_process_files() -> list[str]:\n    """Архивировать и закодировать все файлы из INPUT_DIR."""\n    files = [\n        f for f in os.listdir(INPUT_DIR)\n        if os.path.isfile(os.path.join(INPUT_DIR, f))\n        and not f.startswith(\'.\')\n    ]\n\n    if not files:\n        print("\\n⚠️  Папка input_files/ пуста. Добавьте файлы и запустите снова.\\n")\n        return []\n\n    print(f"\\n📁 Найдено файлов: {len(files)}")\n    encoded_files = []\n\n    for filename in files:\n        file_path = os.path.join(INPUT_DIR, filename)\n        file_size_mb = os.path.getsize(file_path) / (1024 * 1024)\n\n        print(f"\\n{\'─\'*50}")\n        print(f"📄 Обработка: {filename}  ({file_size_mb:.2f} МБ)")\n\n        print("   🗜️  Архивация в RAR...")\n        rar_parts = create_rar_archive(file_path, TEMP_RAR_DIR)\n        print(f"   ✅ Томов создано: {len(rar_parts)}")\n\n        print("   🔒 Кодирование Base64 → .txt...")\n        txt_files = encode_to_txt(rar_parts, ENCODED_DIR)\n        encoded_files.extend(txt_files)\n        print(f"   ✅ .txt готово: {len(txt_files)}")\n\n    print(f"\\n{\'─\'*50}")\n    print(f"✅ Все файлы обработаны! .txt файлы в: {ENCODED_DIR}/")\n    print(f"   Всего .txt: {len(encoded_files)}\\n")\n    return encoded_files\n\n\ndef sender_menu():\n    """Меню отправителя."""\n    encoded_files = sender_process_files()\n    if not encoded_files:\n        return\n\n    print("━" * 52)\n    print("  📤 Что сделать с зашифрованными файлами?")\n    print("━" * 52)\n    print(f"  [1] 📂 Оставить в папке {ENCODED_DIR}/ (взять вручную)")\n    print("  [2] ☁️  Загрузить в GitHub репозиторий")\n    print("  [0] 🚪 Выход")\n    print("━" * 52)\n\n    choice = _ask("Ваш выбор", [\'0\', \'1\', \'2\'])\n\n    if choice == \'0\':\n        print("\\n👋 Выход.\\n"); sys.exit(0)\n    elif choice == \'1\':\n        print(f"\\n📂 Файлы в папке: {ENCODED_DIR}/  — заберите вручную.\\n")\n    elif choice == \'2\':\n        print("\\n☁️  Загрузка в GitHub...")\n        upload_to_github(encoded_files)\n\n\n# ════════════════════════════════════════════════\n#  РЕЖИМ ПОЛУЧАТЕЛЯ\n# ════════════════════════════════════════════════\n\ndef receiver_menu():\n    """Меню получателя: скачать → декодировать → распаковать → очистить."""\n    print("━" * 52)\n    print("  📥 Откуда брать зашифрованные .txt файлы?")\n    print("━" * 52)\n    print("  [1] ☁️  Скачать автоматически с GitHub")\n    print("  [2] 📂 Файлы уже лежат в папке received_txt/")\n    print("  [0] 🚪 Выход")\n    print("━" * 52)\n\n    choice = _ask("Ваш выбор", [\'0\', \'1\', \'2\'])\n\n    if choice == \'0\':\n        print("\\n👋 Выход.\\n"); sys.exit(0)\n\n    elif choice == \'1\':\n        print("\\n☁️  Скачивание с GitHub...")\n        downloaded = download_from_github()\n        if not downloaded:\n            print("⚠️  Нечего скачивать.\\n")\n            return\n\n    # Декодирование и распаковка\n    print("\\n🔓 Декодирование и распаковка...")\n    success = run_decoder()\n\n    if success:\n        ask_clean_repo()\n\n\ndef ask_clean_repo():\n    """Спросить об очистке репозитория после успешного получения."""\n    print("━" * 52)\n    print("  🧹 Файлы успешно распакованы!")\n    print("  Очистить репозиторий GitHub от .txt файлов?")\n    print("━" * 52)\n    print("  [1] 🧹 Да, удалить файлы из репозитория")\n    print("  [2] ⏭️  Нет, оставить как есть")\n    print("━" * 52)\n\n    choice = _ask("Ваш выбор", [\'1\', \'2\'])\n    if choice == \'1\':\n        print("\\n🧹 Очистка репозитория...")\n        clean_github_repo()\n    else:\n        print("\\n⏭️  Репозиторий оставлен без изменений.\\n")\n\n\n# ════════════════════════════════════════════════\n#  УТИЛИТЫ\n# ════════════════════════════════════════════════\n\ndef _ask(prompt: str, options: list[str]) -> str:\n    """Безопасный ввод с проверкой допустимых значений."""\n    opts = \'/\'.join(options)\n    while True:\n        val = input(f"  {prompt} ({opts}): ").strip()\n        if val in options:\n            return val\n        print(f"  ⚠️  Введите одно из: {opts}")\n\n\n# ════════════════════════════════════════════════\n#  ГЛАВНОЕ МЕНЮ\n# ════════════════════════════════════════════════\n\ndef main_menu():\n    print("""\n╔══════════════════════════════════════╗\n║   FileConvert & Sender  v2.1         ║\n║   RAR → Base64 ↔ GitHub  ↔  RAR      ║\n╚══════════════════════════════════════╝""")\n    print("━" * 52)\n    print("  Выберите режим работы:")\n    print("━" * 52)\n    print("  [1] 📤 Отправитель  — упаковать и отправить файлы")\n    print("  [2] 📥 Получатель   — скачать, декодировать, распаковать")\n    print("  [3] 🧹 Очистка репо — удалить все .txt из GitHub вручную")\n    print("  [0] 🚪 Выход")\n    print("━" * 52)\n\n    choice = _ask("Ваш выбор", [\'0\', \'1\', \'2\', \'3\'])\n\n    if   choice == \'0\': print("\\n👋 До свидания!\\n"); sys.exit(0)\n    elif choice == \'1\': sender_menu()\n    elif choice == \'2\': receiver_menu()\n    elif choice == \'3\':\n        print("\\n🧹 Ручная очистка репозитория...")\n        clean_github_repo()\n\n\nif __name__ == \'__main__\':\n    setup_dirs()\n    main_menu()\n'

decoded_rar = 'decoded_rar'
done_files = 'done_files'
encoded_files = 'encoded_files'
input_files = 'input_files'
output_files = 'output_files'
received_txt = 'received_txt'
temp_rar = 'temp_rar'


def restore_project(target_dir="."):
    """Восстанавливает структуру проекта в указанной директории."""
    os.makedirs(target_dir, exist_ok=True)

    # Создаём директории
    os.makedirs(os.path.join(target_dir, decoded_rar), exist_ok=True)
    print(f"Создана директория: {decoded_rar}")
    os.makedirs(os.path.join(target_dir, done_files), exist_ok=True)
    print(f"Создана директория: {done_files}")
    os.makedirs(os.path.join(target_dir, encoded_files), exist_ok=True)
    print(f"Создана директория: {encoded_files}")
    os.makedirs(os.path.join(target_dir, input_files), exist_ok=True)
    print(f"Создана директория: {input_files}")
    os.makedirs(os.path.join(target_dir, output_files), exist_ok=True)
    print(f"Создана директория: {output_files}")
    os.makedirs(os.path.join(target_dir, received_txt), exist_ok=True)
    print(f"Создана директория: {received_txt}")
    os.makedirs(os.path.join(target_dir, temp_rar), exist_ok=True)
    print(f"Создана директория: {temp_rar}")

    # Создаём py файлы
    file_path = os.path.join(target_dir, 'archiver.py')
    with open(file_path, "w", encoding="utf-8") as f:
        f.write(archiver)
    print(f"Создан файл: 'archiver.py'")

    file_path = os.path.join(target_dir, 'config.py')
    with open(file_path, "w", encoding="utf-8") as f:
        f.write(config)
    print(f"Создан файл: 'config.py'")

    file_path = os.path.join(target_dir, 'decoder.py')
    with open(file_path, "w", encoding="utf-8") as f:
        f.write(decoder)
    print(f"Создан файл: 'decoder.py'")

    file_path = os.path.join(target_dir, 'encoder.py')
    with open(file_path, "w", encoding="utf-8") as f:
        f.write(encoder)
    print(f"Создан файл: 'encoder.py'")

    file_path = os.path.join(target_dir, 'main.py')
    with open(file_path, "w", encoding="utf-8") as f:
        f.write(main)
    print(f"Создан файл: 'main.py'")

    print("\nПроект успешно восстановлен!")


if __name__ == "__main__":
    restore_project(".")
